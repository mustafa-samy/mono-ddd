{
  "$schema": "http://json-schema.org/schema",
  "$id": "CodeFormatSchema",
  "title": "Code formatter Schema",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "The path to the domain."
    }
  },
  "required": ["path"]
}
